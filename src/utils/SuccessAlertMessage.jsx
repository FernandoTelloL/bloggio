import Swal from "sweetalert2"
import { useNavigate } from "react-router-dom"
import "animate.css"

export const SuccesAlertMessage = () => {
  const navigate = useNavigate()

  return <h1>hola</h1>
}
