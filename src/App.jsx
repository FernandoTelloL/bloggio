import { RoutesConfig } from './router/RoutesConfig'
import { BrowserRouter } from 'react-router-dom'

function App () {
  return (
    <BrowserRouter>
      <RoutesConfig />
    </BrowserRouter>
  )
}

export default App
